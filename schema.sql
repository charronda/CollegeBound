-- Runt this schema file in mysql workbench first before running the app
-- Drops the college db if it exists currently --
DROP DATABASE IF EXISTS college;
-- Creates the "college" database --
CREATE DATABASE college;